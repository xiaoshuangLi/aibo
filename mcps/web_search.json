{
  "name": "web_search",
  "description": "Perform web searches and fetch content from the internet using advanced search capabilities",
  "endpoint": "https://api.search.example.com",
  "authentication": {
    "type": "none"
  },
  "tools": [
    {
      "name": "perform_search",
      "description": "Perform a web search query",
      "parameters": {
        "required": ["query"],
        "properties": {
          "query": {
            "type": "string",
            "description": "Search query string"
          }
        }
      }
    }
  ]
}