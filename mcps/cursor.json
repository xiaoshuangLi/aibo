{
  "name": "cursor",
  "description": "Cursor IDE MCP Server for autonomous coding capabilities",
  "endpoint": "http://localhost:8080/mcp",
  "authentication": {
    "type": "none"
  },
  "tools": [
    {
      "name": "cursor_edit_code",
      "description": "Edit code using Cursor's autonomous coding capabilities",
      "parameters": {
        "required": ["file_path", "instructions"],
        "properties": {
          "file_path": {
            "type": "string",
            "description": "Path to the file to edit"
          },
          "instructions": {
            "type": "string", 
            "description": "Natural language instructions for the code edit"
          }
        }
      }
    },
    {
      "name": "cursor_generate_code",
      "description": "Generate new code files using Cursor",
      "parameters": {
        "required": ["file_path", "specification"],
        "properties": {
          "file_path": {
            "type": "string",
            "description": "Path for the new file to generate"
          },
          "specification": {
            "type": "string",
            "description": "Specification for the code to generate"
          }
        }
      }
    }
  ]
}